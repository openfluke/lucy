# testdata/

Shared golden vectors for Score / Q / LPD / bands.

Every runtime (Go, wasm via npm, Python binary wrapper) must match these.
When formulas or floors change, update goldens here first — hosts follow.
