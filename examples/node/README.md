# Node / Bun examples (`@openfluke/lucy` API)

```bash
cd examples/node
node 01_wasm_build.mjs      # wasm measuring
node 02_native_full.mjs     # binary: csv + site pdf + charts
node 03_serve_client.mjs    # HTTP client
```

Imports use the local `js/` package (same entry as published npm).
