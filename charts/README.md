# charts/

**0.7.0:** SVG + PNG + JPG + PDF (cover + table + charts) + CSV in reports.

```bash
lucy pdf ./board.pdf < req.json
lucy csv < req.json > board.csv
lucy report ./out < req.json   # index.html + assets + board.pdf + board.csv
```

Full Tide/River site PDF parity → **1.0**.
