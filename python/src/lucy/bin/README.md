# python/src/lucy/bin/

Built locally — **not** committed to git.

```bash
./go/scripts/build-artifacts.sh
# → lucy (+ lucy-<os>-<arch>) here for openfluke-lucy
```

Or download platform binaries from a [GitHub Release](https://github.com/openfluke/lucy/releases).
